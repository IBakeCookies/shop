services:
    app-prod:
        container_name: web-prod
        build:
            context: .
            dockerfile: Dockerfile.prod
        ports:
            - '80:3000'
        restart: unless-stopped
